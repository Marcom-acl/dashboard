# Ce fichier est géré exclusivement par POST /veille-ia/ingest via l'API GitHub REST.
# Protection locale : git update-index --skip-worktree data/veille-ia-data.json
# Ne pas committer manuellement — écrase l'historique accumulé.
